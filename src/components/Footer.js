import React from 'react';

const Footer = () => {
    return (
        <footer style={footerStyle}>
                <h2>LET'S START</h2>
                <h3>UIDE / LibraryThing  / 2024</h3>
        </footer>
    );
};

    const footerStyle = {
        backgroundColor: '#000000', // Color de fondo del footer
        color: '#ffffff', // Color del texto
        padding: '10px',
        textAlign: 'center'
    };

export default Footer;
